#include <stdio.h>

/**
 * main  prints exactly with proper grammar,
 * but the outcome is a piec of art,
 * followed by a new line
 * Return: 0 if successful, non zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
