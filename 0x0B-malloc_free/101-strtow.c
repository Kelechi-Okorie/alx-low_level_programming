#include <stdlib.h>

/**
 * strtow - splits a string into words
 * @str: the string to split
 *
 * Return: pionter to array of strings if successful
 * NULL otherwise
 */
char **strtow(char *str)
{
	char **buffer = NULL;
	int i;
	int j;
	int len;

	len = 0;

	if (str == NULL || str == "")
		return (NULL);

	

	return (buffer);
}
